#include <bits/stdc++.h>
using namespace std;
const int N = 2e5 + 5;
int n, m;
int a[N];
int st_max[N], st_min[N], top_max, top_min, rt;
int L[N], R[N], M[N], id[N], cnt;

int main() {
    ios::sync_with_stdio(false);
    return 0;
}